@model IEnumerable<testuser.Models.SysModel.tblCategorias>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="col-lg-offset-3 col-xs-12 col-md-6">
    <div class="row">
        <div class="box box-primary">
            <div class="box-header">
                <h4 class="box-title text-info"><strong>Categorias de Casos </strong></h4>
                <div class="box-tools">
                    @*<div class="input-group input-group-sm" style="width: 150px;">
                            <input type="text" name="buscar" class="form-control pull-right" placeholder="Search">
                            <div class="input-group-btn">
                                <button type="submit" class="btn btn-default"><i class="fa fa-search"></i></button>
                            </div>
                        </div>*@
                </div>
            </div>
            <div class="box-body table-responsive">
                <p>
                    @Html.ActionLink("Nuevo registro", "Create", new { }, new { @class = "btn bg-navy" })
                </p>
                <table class="table table-striped table-hover table-condensed">
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Nombre_Categoria)
                        </th>
                        <th></th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Nombre_Categoria)
                            </td>
                            <td>
                                @Html.ActionLink(" ", "Edit", new { id = item.Id_Categoria }, new { @class = "fa fa-edit btn bg-teal" }) |

                                @Html.ActionLink(" ", "Details", new { id = item.Id_Categoria }, new { @class = "fa fa-folder-open btn bg-navy" }) |

                                @Html.ActionLink(" ", "Delete", new { id = item.Id_Categoria }, new { @class = "fa fa-trash btn bg-red" })

                            </td>
                        </tr>
                    }

                </table>
            </div>
        </div>
    </div>
</div>